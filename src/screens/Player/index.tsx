import React, { FC } from 'react';

import { Container, Title } from 'components/shared';

const Player: FC = () => {
  return (
    <Container>
      <Title>Player</Title>
    </Container>
  );
};

export default Player;
